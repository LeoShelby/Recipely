<% provide(:title, @user.name) %>
<div class="row">
	<aside class="col-md-4">
		<section class="user_info">
			<h1>
				<%= gravatar_for @user %>   <!-- utilizzo di gravatar per gestire le immagini di profilo, gravatar_for è definito in users_helper -->
				<%= @user.name %>
			</h1>
		</section>
		
		<section class="stats">
			<%= render 'shared/stats' %>  <!-- mostra statistiche di follow e follower del tale utente -->
		</section>
		
	</aside>
	
	
	<div class="col-md-8">
		<%= render 'follow_form' if logged_in? %>
	</div>
</div>
